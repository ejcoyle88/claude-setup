## Scripts
- Reusable, cross-project scripts you write for me go in `$HOME/.claude/scripts/` (create it if missing), with a one-line header comment saying what the script does.
- Project-specific tooling stays in the repo (e.g. `scripts/`), version-controlled with the project — don't move it to the global dir.
